# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: lead-creation.spec.js >> 3. Lead Creation - Dropdown and Picklist Selection >> 3.1 Select Lead Source Dropdown
- Location: tests\lead-creation.spec.js:379:3

# Error details

```
TimeoutError: page.goto: Timeout 30000ms exceeded.
Call log:
  - navigating to "https://nexturninc6.my.salesforce.com/", waiting until "load"

```

# Page snapshot

```yaml
- generic [ref=e1]:
  - generic:
    - banner:
      - generic:
        - generic:
          - generic:
            - generic: Search Salesforce
            - generic:
              - searchbox "Search Salesforce"
        - list:
          - listitem:
            - generic "person name"
      - generic:
        - navigation:
          - list:
            - listitem:
              - generic "Home"
              - generic:
                - generic:
                  - generic: Open menu item submenu
            - listitem
            - listitem
            - listitem
            - listitem
    - generic:
      - generic: Spring '26
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import 'dotenv/config';
  3  | import { waitForSFLoad } from '../utils/sf-helpers.js';
  4  | 
  5  | /**
  6  |  * SEED SPEC — Salesforce Login Fixture
  7  |  * ─────────────────────────────────────
  8  |  * This file is the foundation for all agent-generated tests.
  9  |  * The Playwright Test Agents (Planner, Generator, Healer) read this
  10 |  * and copy the auth pattern into every .spec.js they produce.
  11 |  *
  12 |  * Auth state is written by globalSetup (utils/sf-helpers.js) and
  13 |  * reused here via storageState so no re-login occurs per test.
  14 |  */
  15 | 
  16 | // Reuse the saved Salesforce auth session
  17 | test.use({ storageState: './reports/.auth-state.json' });
  18 | 
  19 | /**
  20 |  * Extended test fixture: sfPage
  21 |  * Provides a pre-authenticated Salesforce Lightning page.
  22 |  * Generator agent should use this in all generated tests.
  23 |  */
  24 | export const sfTest = test.extend({
  25 |   sfPage: async ({ page }, use) => {
> 26 |     await page.goto(process.env.SF_URL);
     |                ^ TimeoutError: page.goto: Timeout 30000ms exceeded.
  27 |     await waitForSFLoad(page);
  28 |     await use(page);
  29 |   },
  30 | });
  31 | 
  32 | /**
  33 |  * Seed health check — verifies the auth setup works.
  34 |  * Agents run this first to confirm login is working before proceeding.
  35 |  */
  36 | test('seed: Salesforce login and Lightning shell loads', async ({ page }) => {
  37 |   await page.goto(process.env.SF_URL);
  38 |   await waitForSFLoad(page);
  39 | 
  40 |   // Lightning navigation bar must be visible
  41 |   await expect(
  42 |     page.locator('[data-id="AppNavigation"], .navContainer, one-app-nav-bar')
  43 |   ).toBeVisible({ timeout: 20000 });
  44 | 
  45 |   // Should NOT be on login page
  46 |   await expect(page).not.toHaveURL(/login/);
  47 | });
  48 | 
```